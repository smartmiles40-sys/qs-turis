# SPRINT 2026-07-14 — Pente fino pré-produção (Parte 1: revisão)

Auditoria completa do QS feita em 2026-07-14 por 5 revisores em paralelo (Leads,
Tarefas/Cadências, Reuniões/Metas/Painel, Configurações/Admin/Auth, Backend/Dados/
Integrações), cruzada com o backlog do `docs/REVISAO-2026-07-13.md` (nada daqui
repete o que já está lá). TypeScript compila limpo (`tsc --noEmit` = 0 erros).

**Placar: 2 P0 · 17 P1 · ~35 P2 · ~35 P3.**
O tema dominante: o app **Cria e Lê bem, mas quase não Edita nem Exclui** — e
quando uma gravação falha, ela **falha em silêncio** (UI mostra sucesso).

Esforço estimado por item: 🟢 pequeno (≤1h) · 🟡 médio (meio dia) · 🔴 grande (1 dia+).

---

## ⚠️ Estado do repo (antes de tudo)

- Há trabalho **não commitado** na working tree: isolamento por dono
  (CommandPalette, CoveragePanel, LeadDetailPage), flag `deduped` no
  `api/lead-inbound.js` e o `supabase/APLICAR-ISOLAMENTO-SDR.sql`. Decidir:
  commitar junto com a sprint (recomendado) ou descartar. **Nada disso está em
  produção.**
- `supabase/APLICAR-ISOLAMENTO-SDR.sql` é a 3ª definição de
  `qs_assign_lead_owner` no repo (0003, 0008, ele) — o estado do banco depende de
  qual foi colado por último. Consolidar como migration numerada. 🟡

---

## 🔥 FASE 0 — Bloqueadores (corrigir ANTES de pôr gente testando)

Bugs que quebram fluxo, perdem lead ou mentem pro usuário. Todos com correção conhecida.

- [ ] **P0 — Coluna `full_name` não existe: provisionamento SIP inteiro morto** 🟢
  `SettingsPage.tsx:1313/1324/1331/1386` consulta `qs_users.full_name`, mas a
  coluna é `name` (0001_init.sql:22) → erro 42703, lista de SDRs sempre vazia,
  admin não mapeia ramal e o `TelefoneOnboarding` **nunca abre pra ninguém**.
  A telefonia BravoTech recém-lançada está quebrada por isso. Trocar por `name`.

- [ ] **P0 — "Com avanço" + cancelar modal = lead órfão invisível** 🟡
  `TasksPanel.tsx:744/756-761/2293/2337` — a classificação de ligação CONCLUI a
  tarefa no banco antes de abrir o modal de reunião; fechar o modal (X/backdrop/
  Cancelar) deixa a tarefa concluída, sem ganho e sem follow-up → o lead some da
  fila de todos para sempre. Ao cancelar, criar follow-up automático ou reverter.

- [ ] **P1 — Ganho com reunião não checa NENHUM erro** 🟡
  `TasksPanel.tsx:891-896` — `completeTask` e o `update({status:"ganho"})` têm
  retorno ignorado; em falha o card some, sai toast verde e o Bitrix move o deal
  pra WON com o QS ainda em prospecção. Checar os dois retornos antes de UI/Bitrix.

- [ ] **P1 — Import CSV: cópia própria de geração de tarefas + erro engolido** 🟡
  `LeadsPage.tsx:1078-1098` — não usa `createCadenceTasks` e não checa o insert →
  lead entra `em_prospeccao` com 0 tarefas e some da fila (reincidência do bug
  crítico nº 3 da revisão passada). Trocar pela função canônica + contar falhas.

- [ ] **P1 — Lead criado manualmente nasce com dono/tarefas errados** 🟡
  Dois fluxos: (a) `LeadsPage.tsx:132-156` — modal sem campo Responsável, o
  trigger round-robin pode dar o lead a OUTRO SDR e ele some da tela do criador;
  (b) `TasksPanel.tsx:2669` — as tarefas usam o `owner_id` do form (pode ser
  null) e não o dono final do lead → tarefas invisíveis pro SDR com a RLS 0008.
  Default `owner_id = currentUser.id` pra SDR + select de responsável pra gestor
  + tarefas com `inserted.owner_id`.

- [ ] **P1 — "Excluir selecionados" finge sucesso pra SDR** 🟢
  `LeadsPage.tsx:159-177/587-592` — RLS `leads_delete` é só gestor; pra SDR o
  delete não casa linha nenhuma e a UI diz "N lead(s) excluído(s)". Esconder o
  botão pra não-gestor e validar com `.delete().select()` vs contagem.

- [ ] **P1 — Admin pode se auto-desativar e se trancar pra fora** 🟢
  `SettingsPage.tsx:590-593` — o badge Status alterna `is_active` sem confirmação
  nem bloqueio do próprio usuário; sendo o único admin = lockout total (só
  service_role salva). Bloquear `u.id === currentUser.id` no client E no
  `api/admin-user.js`, com confirmação pros demais.

- [ ] **P1 — "Salvar no Bitrix" (observação) apaga o texto mesmo quando falhou** 🟢
  `TasksPanel.tsx:812-819` — `persistObservation` retorna void; a UI limpa o
  campo e toasta sucesso incondicionalmente. Retornar boolean e só limpar no ok.

- [ ] **P1 — Editar meta corrompe o período** 🟢
  `GoalsPage.tsx:252` — o update sempre grava `period_start = hoje`; ajustar uma
  meta de julho em agosto faz ela medir agosto e atropelar metas mais novas
  (dedupe do dashboard pega a mais recente). Omitir `period_start` no update.

- [ ] **P1 — Editar cadência com fetch falho pode APAGAR a cadência real** 🟡
  `CadenceCreatePage.tsx:250-254/316-322` — erro no fetch renderiza o form
  default sem aviso com `isEdit=true`; salvar = delete da estrutura real + grava
  esqueleto vazio. Em erro: tela de erro + "Tentar de novo" + bloquear salvar.

- [ ] **P1 — Cadência sem nenhuma atividade é salvável → leads órfãos** 🟢
  `CadenceCreatePage.tsx:296-297` — só valida o nome; cadência com 0 atividades
  gera 0 tarefas e o lead vinculado fica invisível. Bloquear salvar com soma 0.

- [ ] **P1 — Trocar lead de cadência duplica a carga de trabalho** 🟡
  `LeadsPage.tsx:534-563` + `queries.ts:501-541` — revincular não encerra as
  tarefas pendentes da cadência antiga; o SDR vê as duas sequências e contata em
  dobro. Marcar `ignorada` as pendentes não-extra antes de `createCadenceTasks`
  (mesma regra do `handleSaveExtra`), e desabilitar o select durante o loop.

- [ ] **P1 — Pular a última tarefa deixa o lead sem próxima atividade** 🟡
  `TasksPanel.tsx:1630-1640` — `skipTask` não cria follow-up (diferente dos
  desfechos de contato); em cadeia de follow-up o lead sai da fila sem alerta.
  Se for a última pendente do lead: follow-up automático ou confirmação explícita.
  Bônus: motivos "Aguardando retorno"/"Horário inadequado" deveriam REAGENDAR,
  não matar (`TasksPanel.tsx:1629`).

- [ ] **P1 — Usuário desativado continua operando** 🟡
  RLS 0007 checa `is_active` só pra gestor; `chatapp-send`/`bitrix-sync` aceitam
  qualquer JWT válido; a sessão viva do desativado segue lendo/escrevendo leads e
  mandando WhatsApp. Helper `qs_is_active()` nas policies + `ban_duration` no
  GoTrue ao desativar (`api/admin-user.js`).

- [ ] **P1 — Atribuir responsável em massa: falso sucesso + tarefas com dono velho** 🟢
  `LeadsPage.tsx:570-580` — update sem checar `error` (UI muda mesmo se o banco
  recusar) e as tarefas abertas não são reatribuídas (inconsistente com
  `transferLead`). Checar erro + reatribuir tarefas abertas.

- [ ] **P1 — Onboarding de telefone reabre a cada clique ("Fazer depois" não funciona)** 🟢
  `SdrLayout.tsx:435` + `TelefoneOnboarding.tsx:31-44` — a prop `user` é objeto
  novo a cada render e o efeito depende de `[user]` → `setOpen(true)` em loop
  (hoje mascarado pelo P0 do full_name; explode junto com a correção). Depender
  de `user?.id` + lembrar adiamento em sessionStorage.

- [ ] **P1 — Geração inicial de cadência cai em fim de semana** 🟡
  `api/_leads.js:59-84`, `queries.ts:518-537` e a cópia do CSV — `day_number` em
  dias corridos ignora `execution_weekdays`/`offday_policy`; lead de sexta ganha
  "dia 2" no sábado (o item conhecido do backlog cobre só o follow-up; este é o
  agendamento inicial). Pular pro próximo dia permitido ao montar as datas.

- [ ] **P1 — MeetingsPage não espelha NADA no Bitrix** 🟡
  `MeetingsPage.tsx:213-231/241-252` — criar/editar/realizada/no-show/cancelar
  por ali não chama `notifyBitrix` nem grava os campos estruturados
  (`scheduled_by/meeting_owner/client_email/booking_date`) que o fluxo de Ganho
  grava. Reunião agendada pela agenda não move o deal.

---

## 🔘 FASE 1 — Botões que faltam (o pente fino pedido)

### Leads
- [ ] **Editar lead — o maior buraco do app** 🔴 — NENHUM campo cadastral é
  editável (nome, telefone, e-mail, empresa, cargo, origem, cidade…); erro de
  digitação fica pra sempre. `updateQsLead` (queries.ts:187) está pronto e órfão;
  RLS já permite. Botão "Editar" no header do detalhe + modal.
  (`LeadDetailPage.tsx:804-834`)
- [ ] **Excluir/arquivar no detalhe** 🟢 — só existe exclusão em massa na lista;
  o detalhe não tem. Botão (gestor) com confirmação.
- [ ] **Editar/excluir nota** 🟡 — cards sem ação; RLS permite autor/gestor
  (0007:99-104); faltam `updateQsNote`/`deleteQsNote` + menu no card.
  (`LeadDetailPage.tsx:863-889`)
- [ ] **Aba Contatos morta** 🟡 — `qs_contacts` não tem NENHUM caminho de escrita
  no repo; a aba sempre diz "Nenhum contato cadastrado". Criar "Adicionar
  contato" (CRUD básico) ou remover a aba. (`LeadDetailPage.tsx:894-928`)
- [ ] **Valor do negócio impossível de preencher → receita ganha = R$ 0** 🟡 —
  `closed_value` nunca é escrito por nada; `estimated_value` só via Bitrix. Modal
  de Ganho pedindo valor + campo de estimativa no editar lead.
  (`LeadDetailPage.tsx:341-367`, `SdrDashboard.tsx:1058`)
- [ ] **Temperatura não editável nem filtrável** 🟢 — `lead_score` é badge
  read-only do Bitrix; SDR não classifica Quente/Morno/Frio nem filtra na lista.
  (`LeadDetailPage.tsx:1206-1208`)
- [ ] **Desvincular lead de cadência** 🟢 — não existe; junto com o P1 de troca,
  criar "remover da cadência" no detalhe (encerra tarefas pendentes).
- [ ] **Próximas atividades no detalhe do lead** 🟡 — as tasks pendentes são
  buscadas mas só as concluídas aparecem; SDR não vê o próximo passo nem cria
  atividade extra dali. (`LeadDetailPage.tsx:261/652/976`)
- [ ] **Aba Reuniões do lead: mostrar o que foi coletado + ações** 🟡 — hoje só
  status+data (duas vezes); título/local/link/duração/notas somem, e não há
  editar/realizada/no-show/cancelar (só na MeetingsPage, sem link).
  (`LeadDetailPage.tsx:931-962`)
- [ ] **Mesclar duplicados / aviso de duplicado no cadastro** 🟡 — cadastro manual
  não checa telefone/e-mail existente (dois SDRs cadastram o mesmo lead); não há
  UI de merge. Consulta prévia + "já existe, abrir lead". (`LeadsPage.tsx:136-145`,
  `TasksPanel.tsx:2630-2657`)

### Tarefas
- [ ] **Editar/reagendar/adiar tarefa** 🔴 — não existe mudar data/canal/notas;
  únicos desfechos: concluir, pular (mata) ou "pediu retorno". Menu "Adiar para
  amanhã/próximo dia útil" + editar no hero. (`TasksPanel.tsx:1285-1346/1424-1775`)
- [ ] **Desfazer conclusão** 🟡 — tecla "C" conclui na hora, conta no placar e não
  há NENHUM caminho de reverter (nem admin). "Desfazer" no toast (~10s).
  (`TasksPanel.tsx:1126/1541`, `queries.ts:420-451`)
- [ ] **Excluir tarefa extra criada errada** 🟢 — backend permite (policy
  tasks_delete), sem botão.
- [ ] **Atividade extra pra lead ganho/perdido = tarefa invisível** 🟢 — a busca do
  modal lista leads encerrados e a extra criada não passa no filtro da fila.
  Excluir encerrados da busca. (`TasksPanel.tsx:2438-2455/1009-1013`)

### Cadências
- [ ] **Excluir cadência** 🟢 — `deleteQsCadence` pronto e órfão (queries.ts:697);
  único botão do card é "Editar". + confirmação com contagem de leads/tarefas.
  (`CadencesPage.tsx:549-555`)
- [ ] **Duplicar cadência** 🟡 — variação de cadência de 15 dias = redigitar tudo.
  Botão "Duplicar" abrindo o wizard pré-preenchido.
- [ ] **Congelar de verdade (hoje é decorativo)** 🟡 — congelar só muda badge:
  tarefas continuam na fila e cadências congeladas/rascunho seguem selecionáveis
  em TODOS os dropdowns. Filtrar por `disponivel` + decidir o efeito nas tarefas.
  (`CadencesPage.tsx:407-424`, `TasksPanel.tsx:2606-2610`, `LeadsPage.tsx:568`)
- [ ] **Congelar/retomar por card** 🟢 — só existe em massa.
- [ ] **Ver leads da cadência** 🟢 — contadores no card sem link pra lista filtrada.

### Reuniões / Metas / Notificações
- [ ] **Excluir reunião** 🟢 — não existe `.delete()` em `qs_meetings` no src
  inteiro; reunião errada/duplicada fica pra sempre (RLS já permite).
  (`MeetingsPage.tsx:409-486`)
- [ ] **SDR não tem onde ver as próprias reuniões** 🟡 — nav "reunioes" é só
  gestor/admin e o sino não consulta `qs_meetings` → risco real de no-show do
  próprio time. Liberar a agenda pro SDR (RLS já isola por dono) + seção
  "Reuniões de hoje" no sino. (`QsAuthContext.tsx:31`, `NotificationsPanel.tsx:217-287`)
- [ ] **Link da reunião clicável na tabela** 🟢 — `meeting_link` é capturado e
  nunca exibido; pra entrar no Meet o usuário abre o modal de edição e copia na
  mão. (`MeetingsPage.tsx:369-380`)
- [ ] **Remarcar com rastro** 🟡 — não existe status "remarcada"; editar a data
  sobrescreve sem histórico (métrica de remarcação impossível). Status novo ou
  `rescheduled_from`.
- [ ] **Meta de equipe** 🟢 — modal exige responsável; schema permite
  `owner_id null`. Opção "Equipe (todos)". (`GoalsPage.tsx:244/430-435`)
- [ ] **Notificações: marcar como lida / limpar** 🟡 — nada é dispensável; lead
  quente fica 48h no sino já visto. `dismissed_ids` + "limpar" por seção.
- [ ] **Notificação de tarefa navega pra tarefa certa** 🟢 — hoje cai no painel
  genérico; passar `leadId` e abrir o lead. (`NotificationsPanel.tsx:388-398`)

### Usuários / Conta
- [ ] **Trocar a própria senha** 🟡 — inexistente no app (menu do avatar só tem
  "Sair"); todo mundo depende do admin. `auth.updateUser({password})` no menu.
  (`SdrLayout.tsx:403-428`)
- [ ] **Reabrir o onboarding de telefone dispensado** 🟢 — atalho "meu ramal" no
  menu do avatar. (`TelefoneOnboarding.tsx:62/125`)
- [ ] **"Testar conexão" no Webfone** 🟢 — os helpers (`ensureWavoipDevice` +
  `waitForDeviceReady`) já fazem exatamente isso; só falta o botão.
  (`SettingsPage.tsx:1097-1172`)
- [ ] **Seção de status ChatApp/Bitrix nas Configurações** 🟡 — credenciais só via
  n8n/env; admin não tem onde ver se a integração está viva.

---

## 🔇 FASE 2 — Falhas silenciosas (o padrão dominante do app)

Quase toda gravação fora dos modais ignora o `error` do Supabase. Lista de
consertos (todos 🟢, dá pra varrer em bloco):

- [ ] Toggles de usuário/produto com falso sucesso — `SettingsPage.tsx:591/748-751`
  (replicar o padrão do `toggleChannel:866-874`, que reverte + toast).
- [ ] As 5 seções "Salvo ✓" mudas em falha (Horário/Equipe/Webfone/SIP/Onboarding)
  — `SettingsPage.tsx:953/1031/1111/1205/1342` → `if (!ok) notifyError(...)`.
- [ ] `addProduct` sem checar erro e sem disabled (duplo-Enter duplica) —
  `SettingsPage.tsx:739-746`.
- [ ] Excluir meta falha sem toast (meta "reaparece") — `GoalsPage.tsx:277`.
- [ ] Arquivar motivo de perda sem toast — `SettingsPage.tsx:414-418`.
- [ ] `callAdmin` sem try/catch → botão preso em "Salvando..." com rede caída —
  `adminUsers.ts:27-32`.
- [ ] Erro de rede vira "Nenhum lead encontrado"/"Lead não encontrado"/"Nenhuma
  cadência" (estado de erro + "Tentar novamente") — `LeadsPage.tsx:99-102`,
  `LeadDetailPage.tsx:209-215`, `CadencesPage.tsx:174-177`.
- [ ] Dashboard: erros viram zeros que parecem dado real (banner de erro por
  seção; o padrão bom já existe na MeetingsPage) — `SdrDashboard.tsx:636/550`,
  `RankingPanel.tsx:97-99`, `NotificationsPanel.tsx:277-280` (sino falha → "Tudo
  em dia!" falso).
- [ ] `handleSaveExtra`/`handleConfirmMeeting` ignoram falhas dos passos
  secundários (tarefas ressuscitam no refresh de 60s) — `TasksPanel.tsx:956-966`.
- [ ] `transferLead` não checa o insert do handover (notificação "lead recebido"
  não chega) — `queries.ts:281-286`.
- [ ] `logWhatsApp` nunca reporta falha (catch é código morto) —
  `whatsapp.ts:136-150`.
- [ ] Erro transitório de rede DESLOGA o usuário na inicialização (loadProfile
  não distingue "sem perfil" de "query falhou") — `QsAuthContext.tsx:56-62`.
- [ ] Duplo clique sem guarda: Adicionar Anotação (`LeadDetailPage.tsx:318-338`),
  Confirmar Handover do detalhe (`:1535-1544`), re-contato 30/60/90
  (`:1380-1399`), quick-actions de reunião (`MeetingsPage.tsx:234-252`),
  deleteUser (`SettingsPage.tsx:596-600`).

---

## 🔌 FASE 3 — Integrações & consistência de dados

- [ ] **Dedupe do inbound compara valores não normalizados** 🟢 — grava
  `payload.email/phone` cru mas filtra por lowercase/dígitos; retry do n8n com
  formatação diferente duplica o card. Normalizar na gravação E na comparação.
  (`api/_leads.js:182-183` vs `:222-223`)
- [ ] **Lead do webhook nunca vira "em_prospeccao"** 🟢 — nasce `nao_iniciado`
  com cadência+tarefas e nada o promove; filtro e métricas por status mentem pro
  principal canal de entrada. (`api/_leads.js:226`)
- [ ] **Reativar lead não tem evento no Bitrix** 🟢 — deal fica em LOSE enquanto o
  QS trabalha o lead. Novo evento ou nota automática. (`LeadDetailPage.tsx:1286`,
  `api/bitrix-sync.js:22`)
- [ ] **Vincular cadência em massa ressuscita lead fechado** 🟢 — ganho/perdido na
  seleção vira `em_prospeccao` mantendo `loss_reason_id`, sem confirmação e sem
  evento Bitrix. Excluir fechados ou tratar como reativação. (`LeadsPage.tsx:542-545`)
- [ ] **`MESSAGE_TEXT_FIELD = 'text'` nunca foi confirmado** 🟢 — se a API do
  ChatApp espera outro nome, todo envio direto falha. Fazer 1 envio real de teste.
  (`api/_chatapp.js:37`)
- [ ] **chatapp-send sem rate limit/limite de texto/log server-side** 🟡 —
  qualquer autenticado manda texto arbitrário pra qualquer número; chamada direta
  à API não deixa rastro (log é só client-side). (`api/chatapp-send.js:38-78`)
- [ ] **`segment` significa 3 coisas** 🟡 — produto de interesse (TasksPanel),
  fonte do Bitrix (n8n) e segmento do CSV caem na mesma coluna; o gráfico
  "Conversão por fonte" mistura tudo. Coluna própria pra produto.
- [ ] **Workflow aposentado duplicaria tudo se religado** 🟢 — o fluxo por webhook
  nunca marca `bitrix_synced`; religar o polling (como o README sugere) reenvia
  TODO o histórico desde 2026-07-07. Corrigir o README (ou marcar flags no evento).
  (`n8n/README.md:162-164`)
- [ ] **admin-user: criação sem rollback + delete em ordem errada** 🟡 — conta
  auth órfã se o perfil falhar; no delete o perfil sai antes e a falha do
  auth-delete é engolida (sobra credencial válida sem perfil).
  (`api/admin-user.js:81-99/127-128`)
- [ ] **bitrix-sync aceita `bitrix_id` arbitrário** 🟡 — autenticado pode mover
  qualquer deal do funil; resolver o `bitrix_id` server-side pelo `lead_id`.
  (`api/bitrix-sync.js:56-79`)
- [ ] **Configurações de cadência decorativas** 🔴 — `auto_loss_days` (sem motor de
  perda automática), `distribution_mode` (trigger sempre balanceia),
  `offday_policy`, `execution_mode: "ia"` e objetivo "Redirecionar"
  (`redirect_cadence_id` nunca escrito): a UI promete o que não acontece.
  Priorizar `auto_loss_days` ou esconder os controles. (`CadenceCreatePage.tsx:305-311`)
- [ ] **`script_text` escrito e nunca exibido** 🟢 — o gestor escreve roteiro por
  atividade que o SDR nunca vê; plugar no card da tarefa/`defaultText` do
  WhatsAppModal. (`CadenceCreatePage.tsx:345`)
- [ ] **Cadastro manual sem `arrived_at`** 🟢 — modal insere direto (CSV e
  inbound gravam); métricas de chegada caem no fallback. (`LeadsPage.tsx:136-145`)

---

## 📊 FASE 4 — Dashboard confiável

- [ ] **Motivos de Perda ignora TODOS os filtros** 🟢 — all-time do time inteiro
  sob header que promete "{userName} · {período}". (`SdrDashboard.tsx:169-200`)
- [ ] **Cap de 1000 linhas do PostgREST** 🟡 — funil, heatmap, speed-to-lead,
  motivos e MeetingsPage sem `.limit()`/filtro → acima de 1000 linhas os números
  mentem em silêncio. Filtrar no SQL + `count: exact, head: true` onde só conta.
- [ ] **"% da meta" compara período arbitrário com meta mensal** 🟢 — "Hoje" = 3%
  vermelho, "90 dias" = 300%. Prorratear ou esconder fora de "Mês atual".
  (`SdrDashboard.tsx:584-617`)
- [ ] **SLA da Cobertura reescreve a história** 🟡 — base só
  `nao_iniciado/em_prospeccao`: lead que avança sai do denominador e o "% < 5min"
  muda retroativamente. Calcular sobre leads chegados no período.
  (`CoveragePanel.tsx:134-139`)
- [ ] **Cobertura: "Contatar agora" não contata** 🟢 — registra task de ligação
  concluída sem navegar pro lead nem discar; nome do lead não é clicável.
  (`CoveragePanel.tsx:277-322`)
- [ ] **Meta duplicada permitida e telas divergem** 🟢 — Metas lista as duas, o
  dashboard deduplica pra mais recente. Checar duplicata antes do insert.
- [ ] **Meta de usuário desativado infla o dashboard pra sempre** 🟢 — some da
  página Metas (sem como excluir) mas segue somada no "Meta: X".
- [ ] **Range custom mistura UTC e hora local** 🟢 — início em UTC (21h do dia
  anterior em BRT), fim em local. (`SdrDashboard.tsx:472-476`)
- [ ] **"Últimos 7 dias" cobre 8** 🟢 — `setDate(-7)` + hoje inclusivo (idem
  15/30/90). (`SdrDashboard.tsx:448-452`)
- [ ] Auto-refresh do dashboard (TV do time congela) + drill-down nos cards 🟡.

---

## 🧹 FASE 5 — Polish & pequenos (P3, varrer quando sobrar fôlego)

**Leads/Tarefas:** botões Ganho/Perdido visíveis pra lead já fechado (re-dispara
Bitrix); copiar ID Bitrix sem feedback; handover por seleção grava autor errado
(`from_user_id` = quem clicou, não o dono); reunião/atividade extra aceitam data
no passado; modal CSV fechável no meio da importação (segue rodando escondida);
dois botões "Ligar" com stacks diferentes sem rótulo (SIP vs Wavoip); e-mail
marcado "enviado" só por abrir o rascunho `mailto:`; tarefa atrasada
indistinguível de tarefa de hoje (sem data/badge); classificação de ligação
"presa" pausa o realtime da fila (`classifyFor` não limpo no `selectActive` +
atalhos não pausam com ela aberta — este par é quase P2); seleção sobrevive a
filtros (lista e cadências); paginação renderiza 1 botão por página (300 botões
com 3k leads); dias duplicados no builder; excluir dia do builder sem
confirmação; canais instagram/tiktok/youtube fora do builder e sem ícone na
timeline; `distribution_mode` sem opção "desabilitado".

**Config/Auth/UX:** login sem "mostrar senha"; modal de usuário sem validação
client-side; validar início<fim no Horário; webfone reconectar no save (sem
pedir reload); onboarding SIP dropa linha incompleta em silêncio; badge
Ativo/Inativo como botão sem affordance; `confirm()/alert()` nativos no
deleteUser; alterações não salvas descartadas ao trocar de seção; CommandPalette
só busca lead (sem modo comando ">"); busca global quebra com vírgula/parênteses
(vira 400 silencioso) — `CommandPalette.tsx:75-87`; sino navega closer pra tela
sem permissão (`setActiveNav` sem `canAccessNav`) — quase P2; polling do sino
sem guard de aba oculta; toast de erro com `aria-live="polite"` (deveria ser
assertivo); `window.opener` do ChatApp (tabnabbing) + dock fechado focável por
Tab; dois formatos de telefone copiados na mesma feature (com/sem DDI 55);
gestor bloqueado da UI de Configurações que a RLS permite (decidir de propósito).

**Backend/repo:** sem `vercel.json` → sem CSP/HSTS/X-Frame-Options nem
maxDuration; `rest()` sem timeout (`_supabaseAdmin.js:36`); lead-inbound devolve
`err.message` cru e não valida UUIDs; `qs_audit_log` alimentada e sem tela
("quem perdeu esse cliente?" só via SQL); coluna legada `qs_users.password`
(apagar); lixo versionado (`package-lock 2.json`, `package-lock 3.json`,
`vite.config.ts.timestamp-*.mjs`); tipo `Note` sem `tags`; span "predicted"
morto no KPI card; lista de reuniões decrescente enterra a próxima; `serve.cjs`
com path traversal (só local).

---

## 🧭 Nota arquitetural (causa raiz de ~metade disso)

`src/lib/qs/queries.ts` tem **~40 funções órfãs** — TODO o CRUD de cadência,
reunião, meta, nota, lead (incluindo `updateQsLead`, `deleteQsCadence`,
`cancelMeeting`, `fetchLossReasonsStats`, `fetchLeadsCoverage`) pronto, com
tratamento de erro, e **zero chamadores**: os componentes falam com o Supabase
direto, cada um com sua versão (e seus bugs de erro engolido). A cada botão novo
desta sprint, usar a função da camada em vez de reimplementar inline — isso
conserta o botão E reduz o débito. Mesma história nas **colunas órfãs**:
`qs_contacts` (tabela inteira sem escrita), `closed_value`,
`department/company_linkedin/company_size` (exibidas e nunca escritas),
`redirect_cadence_id`, `script_text` (escrita e nunca lida) — cada uma é um
formulário/botão que falta.

## ✅ O que já está bom (verificado)

Typecheck limpo; realtime com cleanup correto em todos os canais;
chatapp-send/bitrix-sync fail-closed; dedupe 23505 do inbound; fuso BRT do
serverless; `closed_at` com fallback sem a 0012; idempotência de
completeTask/skipTask; Wavoip com versão travada e erros traduzidos; segredos
fora do bundle; confirmações e loading nos modais principais (reunião, meta,
usuário); RLS de metas coerente com o menu.

## 📋 Config do Bruno (continua pendente — sem isso o deploy quebra features)

Migrations **0011/0012** no SQL Editor (+ 0008/0010 se faltam); envs Vercel
`N8N_SYNC_BASE`/`N8N_SYNC_SECRET` (+ `INTERNAL_API_SECRET` no header do n8n);
importar/ativar `qs-to-bitrix-webhook` (e **NÃO** religar o `qs-to-bitrix-sync`
aposentado — ver Fase 3); token do webfone em Config→Webfone; link do instalador
BravoTech + mapear ramais (depois de corrigido o P0 do `full_name`).
