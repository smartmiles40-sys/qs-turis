# Sprint 4 (16→17/07) — RELATÓRIO FINAL

> Sprint "parruda" pedida pelo Bruno: analisar e melhorar tudo. A Onda A tinha sido
> feita pela Fable (commit até `31e595f`); esta sessão (Opus) **terminou o que faltava**:
> Ondas B1/B2/B3 + a revisão adversarial (Onda C) com as correções.
> Estado do plano anterior: `docs/SPRINT-2026-07-17-CONTINUACAO.md`.

## Situação
- **8 commits** locais na `main`, à frente do `origin/main` — **NADA foi pushado ainda**
  (deploy = `git push origin main`; aguardando OK do Bruno).
- `npx tsc -b` ✅ · `npm run build` ✅ · `npx oxlint` ✅ em TODAS as ondas.

## O que foi entregue nesta sessão

### Onda A (feita pela Fable — verificada, não refeita)
Compila/builda/linta limpo, sem stubs; features plugadas (algumas via Supabase inline
em vez das funções-helper — dívida arquitetural conhecida, não bug). CRUD de leads/notas/
contatos, Ganho com valor fechado, cadências (excluir/congelar/duplicar), metas de equipe,
dashboard/cobertura. Commits `596610b`→`31e595f`.

### Onda B1 — Reuniões / Notificações (`f87cfb9`)
- Reuniões: **excluir** (≠ cancelar, mede RLS), **remarcar com rastro** (auditoria no notes),
  **próxima no topo**, **link da reunião clicável**, **combobox de lead com busca**,
  quick-actions com `disabled` anti-duplo-clique.
- Sino: navega pro **lead certo**, seção **"Reuniões de hoje"**, **dispensar/limpar** (lido),
  guard de aba oculta no refresh de 60s.
- SDR/closer passam a ver a página **Reuniões** (RLS já isola).

### Onda B2 — Settings / Auth (`c7554d7`)
- **Trocar a própria senha** (modal no menu do avatar).
- **Reabrir o onboarding do telefone** pelo menu do avatar.
- **"Testar conexão"** do webfone (Wavoip e VoxFree).
- **Seção de status** de integrações (ChatApp / Bitrix, read-only).
- FASE 2 do Settings: toggles **revertem + toast** na falha, saves mudos ganharam toast,
  `addProduct` com disabled, `alert()` → `notifyError`, validação client (e-mail/senha/
  início<fim), rota admin em try/catch.
- Login: **mostrar senha**.
- **`loadProfile` resiliente**: distingue **rede-caiu** (não derruba, não acusa "desativado")
  de **sem-perfil** (aí sim desloga). Corrige o bug de deslogar por oscilação de rede.

### Onda B3 — Analytics parte 2 (`5aec44b`)
- **Telefonia real logada** no banco: nova tabela `qs_call_logs` (migration **0020**),
  helper `logCallEnded`, injetado no `handleCallEnded` do Painel (fire-and-forget).
- Novo painel **"Análises Avançadas"** (Desempenho, só gestor/admin): atendimento por horário,
  duração média por SDR, show-rate por fonte + antecedência, speed-to-lead por SDR/fonte,
  funil comparativo de SDRs, R$ por fonte.
- Gestor passou a ver **todo** o grupo Desempenho (faltavam Saúde da Cadência / Análises de FUP).

### Onda C — revisão adversarial + correções (`6bef2e8`)
3 revisores adversariais no diff `88f7dd5..HEAD`. Achados corrigidos:
- **[P0/P1 segurança] `qs_contacts` sem RLS de dono** → migration **0021**. A Onda A ligou o
  CRUD de contatos (telefone/e-mail/WhatsApp — PII), mas a tabela ficou de fora do lockdown
  do 0007; qualquer SDR autenticado lia/editava/apagava contato de lead alheio. **Fechado.**
- **[P1] Sino escondia follow-up atrasado pra sempre** → dispensa de tarefa agora é **por dia**
  (`task:<id>:<data>`, com poda), reaparece no dia seguinte.
- **[P1] Análises Avançadas**: funil contava **reunião cancelada**; SDR **desativado** virava
  "Sem dono"; **R$** ignorava `estimated_value`; conversão do funil podia passar de **100%**.
  Tudo corrigido.
- **[P2]** "Duplicar cadência" quebrava em **dev (StrictMode)**; import CSV podia travar o
  modal (try/finally); combobox de lead fecha ao clicar fora; RLS de insert do 0020 restrita
  ao próprio dono; comentário do boot corrigido.

---

## ⚠️ PENDENTE DO BRUNO (antes/junto do deploy)

### 1. Aplicar as migrations no Supabase (projeto `eabfjomrnucymduqnbci`), em ordem:
- [ ] `0017_bloqueio_usuario_desativado.sql` (da Sprint 3, ainda pendente)
- [ ] `0018_message_log_e_drop_password.sql` (da Onda A)
- [ ] `0020_call_logs.sql` (telefonia — sem ela, o log falha em silêncio e as seções A/B do
      painel ficam vazias; nada quebra)
- [ ] `0021_qs_contacts_rls.sql` (**segurança — aplicar sim ou sim**; fecha o vazamento de
      contatos entre SDRs)

> Obs.: não existem 0016 nem 0019 (pulos de numeração, ok).

### 2. Deploy
- [ ] `git push origin main` (dispara o deploy na Vercel). São **8 commits** novos.

### 3. Config que já estava pendente de sprints anteriores (não muda com esta)
- Envs Vercel `N8N_SYNC_BASE` / `N8N_SYNC_SECRET` (proxy Bitrix); migrations 0011/0012 se
  ainda não aplicadas; token do webfone (Wavoip/VoxFree) e ramais — ver
  `docs/REVISAO-2026-07-13.md` e `docs/WEBFONE-WEBRTC.md`.

## Observações / dívidas deixadas de propósito (fora do escopo desta noite)
- Onda A usa Supabase inline em vez das funções-helper órfãs de `queries.ts` (refactor futuro).
- `provider` do `qs_call_logs` grava null (os dois webfones compartilham um callback só; a
  coluna já está pronta pra diferenciar quando quiser).
- Achados P2 menores não corrigidos: menu ⋯ do card do Painel sem click-outside; visão "Todos"
  do dashboard soma realizado de dono inativo sem a meta dele (edge). Detalhe nos relatórios
  dos revisores desta sessão.
